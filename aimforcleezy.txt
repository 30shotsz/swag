local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local UPDATE_NEWS_LINK = "https://raw.githubusercontent.com/30shotsz/swag/refs/heads/main/newsnewsnews.txt" -- Replace with your raw pastebin/github link

-- Optimized variables
local fovRadius = 150
local locationSpeed = 1
local toggleKey = Enum.KeyCode.Q
local uiToggleKey = Enum.KeyCode.RightShift
local aimHoldKey = Enum.KeyCode.E
local AimbotEnabled = false
local Aimbot360Enabled = false
local teamCheckEnabled = false
local targetPlayer = nil
local targetHead = nil
local listeningForKey = false
local listeningForUIKey = false
local listeningForHoldKey = false
local espMasterEnabled = false
local espShowBoxes = false
local espShowNames = false
local espWallCheck = false
local silentAimEnabled = false
local isDraggingSlider = false
local aimHoldActive = false
local whitelistEnabled = false
local whitelistData = {}
local holdKeyDown = false

-- Color customization
local espBoxColor = Color3.fromRGB(255, 255, 255)
local espBoxFillColor = Color3.fromRGB(255, 255, 255)
local espNameColor = Color3.fromRGB(255, 255, 255)
local chamsFillTransparency = 0.6
local chamsOutlineTransparency = 0

-- UI Theme customization
local uiSettings = {
    background = Color3.fromRGB(12, 12, 16),
    sidebarBackground = Color3.fromRGB(18, 18, 24),
    headerBackground = Color3.fromRGB(24, 24, 32),
    border = Color3.fromRGB(50, 50, 60),
    accentStart = Color3.fromRGB(255, 255, 255),
    accentEnd = Color3.fromRGB(80, 80, 80),
    textPrimary = Color3.fromRGB(255, 255, 255),
    textSecondary = Color3.fromRGB(160, 160, 170),
    activeRed = Color3.fromRGB(200, 200, 200),
    activeGreen = Color3.fromRGB(255, 255, 255),
    activeRedBg = Color3.fromRGB(22, 22, 26),
    buttonBg = Color3.fromRGB(24, 24, 30),
    buttonActiveBg = Color3.fromRGB(240, 240, 250),
    buttonActiveText = Color3.fromRGB(15, 15, 20),
    trackBg = Color3.fromRGB(35, 35, 45),
    inputBg = Color3.fromRGB(10, 10, 10),
    containerBg = Color3.fromRGB(20, 20, 26),
    itemBg = Color3.fromRGB(30, 30, 38),
    itemHoverBg = Color3.fromRGB(50, 50, 60),
    mainTransparency = 0,
    sidebarTransparency = 0,
    headerTransparency = 0,
}

-- UI Settings Storage
local uiStorage = Instance.new("Folder")
uiStorage.Name = "UISettingsStorage"
uiStorage.Parent = LocalPlayer

local function saveUISettings()
    local success, result = pcall(function()
        return HttpService:JSONEncode(uiSettings)
    end)
    if success then
        pcall(function()
            if writefile then
                writefile("zaycleezy_ui_settings.json", result)
            end
        end)
        pcall(function()
            if isfile and makefolder then
                makefolder("zaycleezy_aimbot")
                writefile("zaycleezy_aimbot/ui_settings.json", result)
            end
        end)
        pcall(function()
            local existing = uiStorage:FindFirstChild("UISettingsData")
            if existing then existing:Destroy() end
            local storage = Instance.new("StringValue")
            storage.Name = "UISettingsData"
            storage.Value = result
            storage.Parent = uiStorage
        end)
    end
end

local function loadUISettings()
    local loaded = false
    
    pcall(function()
        if isfile and isfile("zaycleezy_aimbot/ui_settings.json") then
            local data = readfile("zaycleezy_aimbot/ui_settings.json")
            local success, result = pcall(function()
                return HttpService:JSONDecode(data)
            end)
            if success and type(result) == "table" then
                for k, v in pairs(result) do
                    uiSettings[k] = v
                end
                loaded = true
            end
        end
    end)
    
    if not loaded then
        pcall(function()
            if isfile and isfile("zaycleezy_ui_settings.json") then
                local data = readfile("zaycleezy_ui_settings.json")
                local success, result = pcall(function()
                    return HttpService:JSONDecode(data)
                end)
                if success and type(result) == "table" then
                    for k, v in pairs(result) do
                        uiSettings[k] = v
                    end
                    loaded = true
                end
            end
        end)
    end
    
    if not loaded then
        local storage = uiStorage:FindFirstChild("UISettingsData")
        if storage then
            local success, result = pcall(function()
                return HttpService:JSONDecode(storage.Value)
            end)
            if success and type(result) == "table" then
                for k, v in pairs(result) do
                    uiSettings[k] = v
                end
            end
        end
    end
end
loadUISettings()

-- Cache for performance
local vector3New = Vector3.new
local cframeLookAt = CFrame.lookAt
local tableCreate = table.create
local mathHuge = math.huge
local mathClamp = math.clamp
local mathRound = math.round

-- ==================== PERSISTENT WHITELIST STORAGE ====================
local whitelistStorage

local function saveWhitelist()
    local success, result = pcall(function()
        return HttpService:JSONEncode(whitelistData)
    end)
    if success then
        pcall(function()
            if writefile then
                writefile("zaycleezy_whitelist.json", result)
            end
        end)
        pcall(function()
            if isfile and makefolder then
                makefolder("zaycleezy_aimbot")
                writefile("zaycleezy_aimbot/whitelist.json", result)
            end
        end)
        pcall(function()
            local existing = whitelistStorage:FindFirstChild("WhitelistData")
            if existing then existing:Destroy() end
            local storage = Instance.new("StringValue")
            storage.Name = "WhitelistData"
            storage.Value = result
            storage.Parent = whitelistStorage
        end)
    end
end

local function loadWhitelist()
    local loaded = false
    
    pcall(function()
        if isfile and isfile("zaycleezy_aimbot/whitelist.json") then
            local data = readfile("zaycleezy_aimbot/whitelist.json")
            local success, result = pcall(function()
                return HttpService:JSONDecode(data)
            end)
            if success and type(result) == "table" then
                whitelistData = result
                loaded = true
            end
        end
    end)
    
    if not loaded then
        pcall(function()
            if isfile and isfile("zaycleezy_whitelist.json") then
                local data = readfile("zaycleezy_whitelist.json")
                local success, result = pcall(function()
                    return HttpService:JSONDecode(data)
                end)
                if success and type(result) == "table" then
                    whitelistData = result
                    loaded = true
                end
            end
        end)
    end
    
    if not loaded then
        local storage = whitelistStorage:FindFirstChild("WhitelistData")
        if storage then
            local success, result = pcall(function()
                return HttpService:JSONDecode(storage.Value)
            end)
            if success and type(result) == "table" then
                whitelistData = result
                loaded = true
            end
        end
    end
end

whitelistStorage = Instance.new("Folder")
whitelistStorage.Name = "WhitelistStorage"
whitelistStorage.Parent = LocalPlayer

pcall(function()
    if makefolder then
        makefolder("zaycleezy_aimbot")
    end
end)

loadWhitelist()

if setfpscap then
    pcall(function() setfpscap(9999) end)
end

local Theme = {
    FontBold           = Enum.Font.GothamBold,
    FontMedium         = Enum.Font.GothamMedium,
    FontRegular        = Enum.Font.Gotham,
}

-- ScreenGui Setup
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "OptimizedAimbot"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 99999999
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- FOV Circle
local FOVCircle = Instance.new("Frame")
FOVCircle.Name = "FOVCircle"
FOVCircle.AnchorPoint = Vector2.new(0.5, 0.5)
FOVCircle.Position = UDim2.fromScale(0.5, 0.5)
FOVCircle.Size = UDim2.fromOffset(fovRadius * 2, fovRadius * 2)
FOVCircle.BackgroundTransparency = 1
FOVCircle.Parent = ScreenGui

local UICornerFOV = Instance.new("UICorner")
UICornerFOV.CornerRadius = UDim.new(1, 0)
UICornerFOV.Parent = FOVCircle

local UIStrokeFOV = Instance.new("UIStroke")
UIStrokeFOV.Color = uiSettings.accentStart
UIStrokeFOV.Thickness = 1.5
UIStrokeFOV.Transparency = 0.5
UIStrokeFOV.Parent = FOVCircle

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.fromOffset(480, 440)
MainFrame.Position = UDim2.fromScale(0.1, 0.25)
MainFrame.BackgroundColor3 = uiSettings.background
MainFrame.BackgroundTransparency = uiSettings.mainTransparency
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = uiSettings.accentStart
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

local MainStrokeGradient = Instance.new("UIGradient")
MainStrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, uiSettings.accentStart),
    ColorSequenceKeypoint.new(1, uiSettings.accentEnd)
})
MainStrokeGradient.Rotation = 45
MainStrokeGradient.Parent = MainStroke

-- Header
local HeaderFrame = Instance.new("Frame")
HeaderFrame.Name = "HeaderFrame"
HeaderFrame.Size = UDim2.new(1, 0, 0, 42)
HeaderFrame.BackgroundColor3 = uiSettings.headerBackground
HeaderFrame.BackgroundTransparency = uiSettings.headerTransparency
HeaderFrame.BorderSizePixel = 0
HeaderFrame.Parent = MainFrame

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 10)
HeaderCorner.Parent = HeaderFrame

local HeaderOverlap = Instance.new("Frame")
HeaderOverlap.Size = UDim2.new(1, 0, 0, 10)
HeaderOverlap.Position = UDim2.new(0, 0, 1, -10)
HeaderOverlap.BackgroundColor3 = uiSettings.headerBackground
HeaderOverlap.BackgroundTransparency = uiSettings.headerTransparency
HeaderOverlap.BorderSizePixel = 0
HeaderOverlap.Parent = HeaderFrame

local HeaderDivider = Instance.new("Frame")
HeaderDivider.Size = UDim2.new(1, 0, 0, 1)
HeaderDivider.Position = UDim2.new(0, 0, 1, 0)
HeaderDivider.BackgroundColor3 = uiSettings.border
HeaderDivider.BorderSizePixel = 0
HeaderDivider.Parent = HeaderFrame

local HeaderDividerGradient = Instance.new("UIGradient")
HeaderDividerGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, uiSettings.accentStart),
    ColorSequenceKeypoint.new(1, uiSettings.accentEnd)
})
HeaderDividerGradient.Rotation = 0
HeaderDividerGradient.Parent = HeaderDivider

local HeaderLabel = Instance.new("TextLabel")
HeaderLabel.Size = UDim2.new(1, -50, 1, 0)
HeaderLabel.Position = UDim2.fromOffset(15, 0)
HeaderLabel.BackgroundTransparency = 1
HeaderLabel.Text = "optimized aimbot by zaycleezy"
HeaderLabel.TextColor3 = uiSettings.textPrimary
HeaderLabel.Font = Theme.FontBold
HeaderLabel.TextSize = 14
HeaderLabel.TextXAlignment = Enum.TextXAlignment.Left
HeaderLabel.Parent = HeaderFrame

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Size = UDim2.fromOffset(30, 30)
CloseBtn.Position = UDim2.new(1, -36, 0.5, -15)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "×"
CloseBtn.TextColor3 = uiSettings.textSecondary
CloseBtn.Font = Theme.FontBold
CloseBtn.TextSize = 22
CloseBtn.Active = true
CloseBtn.Parent = HeaderFrame

-- Draggable Functions
local function makeFrameDraggable(dragHandle, targetFrame)
    local dragToggle = false
    local dragStart = nil
    local startPos = nil
    local dragInput = nil

    dragHandle.InputBegan:Connect(function(input)
        if isDraggingSlider then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragToggle = true
            dragStart = input.Position
            startPos = targetFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
            end)
        end
    end)
    dragHandle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragToggle then
            local delta = input.Position - dragStart
            targetFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

local function makeDraggable(frame, clickCallback)
    local dragToggle = false
    local dragStart = nil
    local startPos = nil
    local dragInput = nil
    local isDragging = false

    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragToggle = true
            isDragging = false
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragToggle then
            local delta = input.Position - dragStart
            if delta.Magnitude > 5 then isDragging = true end
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    if clickCallback then
        frame.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                if not isDragging then clickCallback() end
            end
        end)
    end
end

-- Open Button
local OpenBtn = Instance.new("TextButton")
OpenBtn.Name = "OpenBtn"
OpenBtn.Size = UDim2.fromOffset(90, 36)
OpenBtn.Position = UDim2.new(0.5, -45, 0, 20)
OpenBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
OpenBtn.Text = "open ui"
OpenBtn.TextColor3 = uiSettings.textPrimary
OpenBtn.Font = Theme.FontBold
OpenBtn.TextSize = 13
OpenBtn.Visible = false
OpenBtn.Active = true
OpenBtn.Parent = ScreenGui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(0, 8)
OpenCorner.Parent = OpenBtn

local OpenStroke = Instance.new("UIStroke")
OpenStroke.Color = uiSettings.accentStart
OpenStroke.Thickness = 1.5
OpenStroke.Parent = OpenBtn

OpenBtn.MouseEnter:Connect(function()
    TweenService:Create(OpenBtn, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(40, 40, 48) }):Play()
end)
OpenBtn.MouseLeave:Connect(function()
    TweenService:Create(OpenBtn, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(20, 20, 24) }):Play()
end)

-- Floating Aim Button
local FloatingAimBtn = Instance.new("TextButton")
FloatingAimBtn.Name = "FloatingAimBtn"
FloatingAimBtn.Size = UDim2.fromOffset(55, 55)
FloatingAimBtn.Position = UDim2.new(0.85, 0, 0.6, 0)
FloatingAimBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
FloatingAimBtn.Text = "AIM"
FloatingAimBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
FloatingAimBtn.Font = Theme.FontBold
FloatingAimBtn.TextSize = 14
FloatingAimBtn.Visible = false
FloatingAimBtn.Active = true
FloatingAimBtn.Parent = ScreenGui

local FloatingCorner = Instance.new("UICorner")
FloatingCorner.CornerRadius = UDim.new(1, 0)
FloatingCorner.Parent = FloatingAimBtn

local FloatingStroke = Instance.new("UIStroke")
FloatingStroke.Color = uiSettings.accentStart
FloatingStroke.Thickness = 2
FloatingStroke.Parent = FloatingAimBtn

-- Sidebar
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Size = UDim2.new(0, 120, 1, -42)
Sidebar.Position = UDim2.fromOffset(0, 42)
Sidebar.BackgroundColor3 = uiSettings.sidebarBackground
Sidebar.BackgroundTransparency = uiSettings.sidebarTransparency
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainFrame

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 10)
SidebarCorner.Parent = Sidebar

local SidebarOverlap = Instance.new("Frame")
SidebarOverlap.Size = UDim2.new(0, 20, 1, 0)
SidebarOverlap.Position = UDim2.new(1, -20, 0, 0)
SidebarOverlap.BackgroundColor3 = uiSettings.sidebarBackground
SidebarOverlap.BackgroundTransparency = uiSettings.sidebarTransparency
SidebarOverlap.BorderSizePixel = 0
SidebarOverlap.Parent = Sidebar

local SidebarOverlapBottom = Instance.new("Frame")
SidebarOverlapBottom.Size = UDim2.new(1, 0, 0, 10)
SidebarOverlapBottom.Position = UDim2.new(0, 0, 1, -10)
SidebarOverlapBottom.BackgroundColor3 = uiSettings.sidebarBackground
SidebarOverlapBottom.BackgroundTransparency = uiSettings.sidebarTransparency
SidebarOverlapBottom.BorderSizePixel = 0
SidebarOverlapBottom.Parent = Sidebar

local SidebarDivider = Instance.new("Frame")
SidebarDivider.Size = UDim2.new(0, 1, 1, 0)
SidebarDivider.Position = UDim2.new(1, 0, 0, 0)
SidebarDivider.BackgroundColor3 = uiSettings.border
SidebarDivider.BorderSizePixel = 0
SidebarDivider.Parent = Sidebar

-- Content Container
local ContentContainer = Instance.new("Frame")
ContentContainer.Size = UDim2.new(1, -135, 1, -55)
ContentContainer.Position = UDim2.fromOffset(130, 50)
ContentContainer.BackgroundTransparency = 1
ContentContainer.Parent = MainFrame

local function createPage(parent)
    local page = Instance.new("Frame")
    page.Size = UDim2.fromScale(1, 1)
    page.BackgroundTransparency = 1
    page.Parent = parent
    return page
end

local AimbotPage  = createPage(ContentContainer)
AimbotPage.Visible  = true
local VisualsPage = createPage(ContentContainer)
VisualsPage.Visible = false
local WhitelistPage = createPage(ContentContainer)
WhitelistPage.Visible = false
local UICustomPage = createPage(ContentContainer)
UICustomPage.Visible = false
local UpdatesPage = createPage(ContentContainer)
UpdatesPage.Visible = false

local isAnimating = false
local originalSize = UDim2.fromOffset(480, 440)

-- Menu Animation Functions
local function closeMenu()
    if isAnimating then return end
    isAnimating = true
    local currentPos = MainFrame.Position
    local closeTween = TweenService:Create(MainFrame,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        {
            Size = UDim2.fromOffset(0, 0),
            Position = UDim2.new(currentPos.X.Scale, currentPos.X.Offset + (originalSize.X.Offset/2),
                                  currentPos.Y.Scale, currentPos.Y.Offset + (originalSize.Y.Offset/2))
        })
    closeTween.Completed:Connect(function()
        MainFrame.Visible = false
        MainFrame.Size = originalSize
        MainFrame.Position = currentPos
        isAnimating = false
        OpenBtn.Size = UDim2.fromOffset(0, 0)
        OpenBtn.Visible = true
        TweenService:Create(OpenBtn, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
            { Size = UDim2.fromOffset(90, 36) }):Play()
    end)
    closeTween:Play()
end

local function openMenu()
    if isAnimating then return end
    isAnimating = true
    local targetPos = MainFrame.Position
    MainFrame.Size = UDim2.fromOffset(0, 0)
    MainFrame.Position = UDim2.new(targetPos.X.Scale, targetPos.X.Offset + (originalSize.X.Offset/2),
                                    targetPos.Y.Scale, targetPos.Y.Offset + (originalSize.Y.Offset/2))
    MainFrame.Visible = true
    local openTween = TweenService:Create(MainFrame,
        TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        { Size = originalSize, Position = targetPos })
    openTween.Completed:Connect(function() isAnimating = false end)
    openTween:Play()
    local openShrink = TweenService:Create(OpenBtn,
        TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        { Size = UDim2.fromOffset(0, 0) })
    openShrink.Completed:Connect(function()
        OpenBtn.Visible = false
        OpenBtn.Size = UDim2.fromOffset(90, 36)
    end)
    openShrink:Play()
end

CloseBtn.MouseButton1Click:Connect(closeMenu)
makeFrameDraggable(HeaderFrame, MainFrame)
makeFrameDraggable(Sidebar, MainFrame)
makeDraggable(OpenBtn, openMenu)

-- Function to update all UI elements
local function refreshAllUI()
    UIStrokeFOV.Color = uiSettings.accentStart
    MainFrame.BackgroundColor3 = uiSettings.background
    MainFrame.BackgroundTransparency = uiSettings.mainTransparency
    MainStroke.Color = uiSettings.accentStart
    MainStrokeGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, uiSettings.accentStart),
        ColorSequenceKeypoint.new(1, uiSettings.accentEnd)
    })
    HeaderFrame.BackgroundColor3 = uiSettings.headerBackground
    HeaderFrame.BackgroundTransparency = uiSettings.headerTransparency
    HeaderOverlap.BackgroundColor3 = uiSettings.headerBackground
    HeaderOverlap.BackgroundTransparency = uiSettings.headerTransparency
    HeaderDivider.BackgroundColor3 = uiSettings.border
    HeaderDividerGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, uiSettings.accentStart),
        ColorSequenceKeypoint.new(1, uiSettings.accentEnd)
    })
    HeaderLabel.TextColor3 = uiSettings.textPrimary
    CloseBtn.TextColor3 = uiSettings.textSecondary
    OpenStroke.Color = uiSettings.accentStart
    OpenBtn.TextColor3 = uiSettings.textPrimary
    FloatingStroke.Color = uiSettings.accentStart
    Sidebar.BackgroundColor3 = uiSettings.sidebarBackground
    Sidebar.BackgroundTransparency = uiSettings.sidebarTransparency
    SidebarOverlap.BackgroundColor3 = uiSettings.sidebarBackground
    SidebarOverlap.BackgroundTransparency = uiSettings.sidebarTransparency
    SidebarOverlapBottom.BackgroundColor3 = uiSettings.sidebarBackground
    SidebarOverlapBottom.BackgroundTransparency = uiSettings.sidebarTransparency
    SidebarDivider.BackgroundColor3 = uiSettings.border
end

-- UI Component Functions
local function createButton(parent, text, pos, size)
    local btn = Instance.new("TextButton")
    btn.Size = size
    btn.Position = pos
    btn.BackgroundColor3 = uiSettings.buttonBg
    btn.Text = text
    btn.TextColor3 = uiSettings.textPrimary
    btn.Font = Theme.FontMedium
    btn.TextSize = 12
    btn.AutoButtonColor = false
    btn.Active = true
    btn.Parent = parent

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    local stroke = Instance.new("UIStroke")
    stroke.Color = uiSettings.border
    stroke.Thickness = 1
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Parent = btn

    local strokeGradient = Instance.new("UIGradient")
    strokeGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, uiSettings.accentStart),
        ColorSequenceKeypoint.new(1, uiSettings.accentEnd)
    })
    strokeGradient.Rotation = 45
    strokeGradient.Enabled = false
    strokeGradient.Parent = stroke

    btn:GetPropertyChangedSignal("BackgroundColor3"):Connect(function()
        local color = btn.BackgroundColor3
        if color == Color3.fromRGB(0, 120, 200) or color == Color3.fromRGB(0, 162, 255) then
            btn.BackgroundColor3 = uiSettings.buttonActiveBg
            btn.TextColor3 = uiSettings.buttonActiveText
            stroke.Thickness = 1.5
            strokeGradient.Enabled = true
        elseif color == Color3.fromRGB(30, 30, 40) or color == Color3.fromRGB(20, 20, 26) or color == uiSettings.buttonBg then
            btn.BackgroundColor3 = uiSettings.buttonBg
            btn.TextColor3 = uiSettings.textPrimary
            stroke.Color = uiSettings.border
            stroke.Thickness = 1
            strokeGradient.Enabled = false
        elseif color == Color3.fromRGB(50, 40, 40) then
            btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
            btn.TextColor3 = uiSettings.textPrimary
            stroke.Color = Color3.fromRGB(150, 150, 160)
            stroke.Thickness = 1
            strokeGradient.Enabled = false
        end
    end)

    btn.MouseEnter:Connect(function()
        TweenService:Create(stroke, TweenInfo.new(0.2), { Color = uiSettings.accentStart }):Play()
    end)
    btn.MouseLeave:Connect(function()
        if strokeGradient.Enabled then
            TweenService:Create(stroke, TweenInfo.new(0.2), { Color = uiSettings.accentStart }):Play()
        else
            TweenService:Create(stroke, TweenInfo.new(0.2), { Color = uiSettings.border }):Play()
        end
    end)

    return btn
end

local function makeTabButton(btn)
    btn.TextColor3 = uiSettings.textSecondary
    btn:GetPropertyChangedSignal("BackgroundColor3"):Connect(function()
        local color = btn.BackgroundColor3
        if color == uiSettings.accentStart or color == uiSettings.buttonActiveBg then
            btn.TextColor3 = uiSettings.buttonActiveText
        else
            btn.TextColor3 = uiSettings.textSecondary
        end
    end)
end

local function createSlider(parent, text, pos, defaultVal, maxVal, callback)
    local sliderContainer = Instance.new("Frame")
    sliderContainer.Size = UDim2.new(1, -10, 0, 45)
    sliderContainer.Position = pos
    sliderContainer.BackgroundTransparency = 1
    sliderContainer.Parent = parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 20)
    label.Position = UDim2.fromScale(0, 0)
    label.BackgroundTransparency = 1
    label.Text = text .. ": " .. defaultVal
    label.TextColor3 = uiSettings.textSecondary
    label.Font = Theme.FontMedium
    label.TextSize = 11
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = sliderContainer

    local track = Instance.new("Frame")
    track.Size = UDim2.new(1, 0, 0, 6)
    track.Position = UDim2.fromOffset(0, 26)
    track.BackgroundColor3 = uiSettings.trackBg
    track.BorderSizePixel = 0
    track.Active = true
    track.Parent = sliderContainer

    local trackCorner = Instance.new("UICorner")
    trackCorner.CornerRadius = UDim.new(1, 0)
    trackCorner.Parent = track

    local fill = Instance.new("Frame")
    fill.Size = UDim2.fromScale(defaultVal / maxVal, 1)
    fill.BackgroundColor3 = uiSettings.accentStart
    fill.BorderSizePixel = 0
    fill.Parent = track

    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(1, 0)
    fillCorner.Parent = fill

    local knob = Instance.new("Frame")
    knob.Size = UDim2.fromOffset(12, 12)
    knob.AnchorPoint = Vector2.new(0.5, 0.5)
    knob.Position = UDim2.fromScale(defaultVal / maxVal, 0.5)
    knob.BackgroundColor3 = uiSettings.textPrimary
    knob.BorderSizePixel = 0
    knob.Active = true
    knob.Parent = track

    local knobCorner = Instance.new("UICorner")
    knobCorner.CornerRadius = UDim.new(1, 0)
    knobCorner.Parent = knob

    local draggingSlider = false
    local function updateSlider(input)
        local relativeX = input.Position.X - track.AbsolutePosition.X
        local percentage = mathClamp(relativeX / track.AbsoluteSize.X, 0, 1)
        knob.Position = UDim2.fromScale(percentage, 0.5)
        fill.Size = UDim2.fromScale(percentage, 1)
        local calculatedValue = callback(percentage)
        label.Text = text .. ": " .. calculatedValue
    end

    knob.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingSlider = true
            isDraggingSlider = true
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if draggingSlider and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingSlider = false
            isDraggingSlider = false
        end
    end)
end

-- Gradient color picker function
local function createGradientPicker(parent, text, pos, defaultColor, callback)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, -10, 0, 80)
    container.Position = pos
    container.BackgroundTransparency = 1
    container.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 18)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = uiSettings.textSecondary
    label.Font = Theme.FontMedium
    label.TextSize = 11
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container
    
    -- Color preview box
    local preview = Instance.new("Frame")
    preview.Size = UDim2.new(1, 0, 0, 25)
    preview.Position = UDim2.fromOffset(0, 20)
    preview.BackgroundColor3 = defaultColor
    preview.Parent = container
    
    local previewCorner = Instance.new("UICorner")
    previewCorner.CornerRadius = UDim.new(0, 4)
    previewCorner.Parent = preview
    
    -- Hue gradient bar
    local hueBar = Instance.new("Frame")
    hueBar.Size = UDim2.new(1, 0, 0, 12)
    hueBar.Position = UDim2.fromOffset(0, 50)
    hueBar.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    hueBar.BorderSizePixel = 0
    hueBar.Active = true
    hueBar.Parent = container
    
    local hueBarCorner = Instance.new("UICorner")
    hueBarCorner.CornerRadius = UDim.new(0, 6)
    hueBarCorner.Parent = hueBar
    
    local hueGradient = Instance.new("UIGradient")
    hueGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
        ColorSequenceKeypoint.new(0.17, Color3.fromRGB(255, 255, 0)),
        ColorSequenceKeypoint.new(0.33, Color3.fromRGB(0, 255, 0)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)),
        ColorSequenceKeypoint.new(0.67, Color3.fromRGB(0, 0, 255)),
        ColorSequenceKeypoint.new(0.83, Color3.fromRGB(255, 0, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0)),
    })
    hueGradient.Parent = hueBar
    
    local hueKnob = Instance.new("Frame")
    hueKnob.Size = UDim2.fromOffset(6, 16)
    hueKnob.AnchorPoint = Vector2.new(0.5, 0.5)
    hueKnob.Position = UDim2.fromScale(0, 0.5)
    hueKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    hueKnob.BorderSizePixel = 0
    hueKnob.Parent = hueBar
    
    local hueKnobCorner = Instance.new("UICorner")
    hueKnobCorner.CornerRadius = UDim.new(0, 3)
    hueKnobCorner.Parent = hueKnob
    
    local hueKnobStroke = Instance.new("UIStroke")
    hueKnobStroke.Color = Color3.fromRGB(0, 0, 0)
    hueKnobStroke.Thickness = 1
    hueKnobStroke.Parent = hueKnob
    
    -- Sat/Val gradient
    local svGradient = Instance.new("Frame")
    svGradient.Size = UDim2.new(1, 0, 0, 12)
    svGradient.Position = UDim2.fromOffset(0, 66)
    svGradient.BackgroundColor3 = defaultColor
    svGradient.BorderSizePixel = 0
    svGradient.Active = true
    svGradient.Parent = container
    
    local svGradientCorner = Instance.new("UICorner")
    svGradientCorner.CornerRadius = UDim.new(0, 6)
    svGradientCorner.Parent = svGradient
    
    local whiteGradient = Instance.new("UIGradient")
    whiteGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(1, defaultColor),
    })
    whiteGradient.Parent = svGradient
    
    local blackGradient = Instance.new("UIGradient")
    blackGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
    })
    blackGradient.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0),
        NumberSequenceKeypoint.new(1, 1),
    })
    blackGradient.Rotation = 90
    blackGradient.Parent = svGradient
    
    local svKnob = Instance.new("Frame")
    svKnob.Size = UDim2.fromOffset(6, 16)
    svKnob.AnchorPoint = Vector2.new(0.5, 0.5)
    svKnob.Position = UDim2.fromScale(1, 0.5)
    svKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    svKnob.BorderSizePixel = 0
    svKnob.Parent = svGradient
    
    local svKnobCorner = Instance.new("UICorner")
    svKnobCorner.CornerRadius = UDim.new(0, 3)
    svKnobCorner.Parent = svKnob
    
    local svKnobStroke = Instance.new("UIStroke")
    svKnobStroke.Color = Color3.fromRGB(0, 0, 0)
    svKnobStroke.Thickness = 1
    svKnobStroke.Parent = svKnob
    
    local currentColor = defaultColor
    local currentHue = 0
    local currentSat = 1
    local currentVal = 1
    
    local function updateFromHSV()
        local color = Color3.fromHSV(currentHue, currentSat, currentVal)
        currentColor = color
        preview.BackgroundColor3 = color
        callback(color)
    end
    
    local function updateSVBar()
        local baseColor = Color3.fromHSV(currentHue, 1, 1)
        svGradient.BackgroundColor3 = baseColor
        whiteGradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
            ColorSequenceKeypoint.new(1, baseColor),
        })
    end
    
    -- Hue bar interaction
    local draggingHue = false
    hueBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingHue = true
            isDraggingSlider = true
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if draggingHue and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local relativeX = mathClamp((input.Position.X - hueBar.AbsolutePosition.X) / hueBar.AbsoluteSize.X, 0, 1)
            hueKnob.Position = UDim2.fromScale(relativeX, 0.5)
            currentHue = relativeX
            updateSVBar()
            updateFromHSV()
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingHue = false
            isDraggingSlider = false
        end
    end)
    
    -- SV bar interaction
    local draggingSV = false
    svGradient.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingSV = true
            isDraggingSlider = true
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if draggingSV and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local relativeX = mathClamp((input.Position.X - svGradient.AbsolutePosition.X) / svGradient.AbsoluteSize.X, 0, 1)
            svKnob.Position = UDim2.fromScale(relativeX, 0.5)
            currentSat = relativeX
            updateFromHSV()
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingSV = false
            isDraggingSlider = false
        end
    end)
    
    -- Initialize HSV from default color
    local h, s, v = defaultColor:ToHSV()
    currentHue = h
    currentSat = s
    currentVal = v
    hueKnob.Position = UDim2.fromScale(h, 0.5)
    svKnob.Position = UDim2.fromScale(s, 0.5)
    updateSVBar()
end

-- Tab Buttons
local AimbotTabBtn  = createButton(Sidebar, "Aimbot",       UDim2.fromOffset(10, 15),  UDim2.fromOffset(100, 25))
AimbotTabBtn.BackgroundColor3 = uiSettings.accentStart
makeTabButton(AimbotTabBtn)

local VisualsTabBtn = createButton(Sidebar, "Visuals", UDim2.fromOffset(10, 44),  UDim2.fromOffset(100, 25))
makeTabButton(VisualsTabBtn)

local WhitelistTabBtn = createButton(Sidebar, "Whitelist", UDim2.fromOffset(10, 73), UDim2.fromOffset(100, 25))
makeTabButton(WhitelistTabBtn)

local UICustomTabBtn = createButton(Sidebar, "UI Custom", UDim2.fromOffset(10, 102), UDim2.fromOffset(100, 25))
makeTabButton(UICustomTabBtn)

local UpdatesTabBtn = createButton(Sidebar, "Updates", UDim2.fromOffset(10, 131), UDim2.fromOffset(100, 25))
makeTabButton(UpdatesTabBtn)

local function resetTabs()
    AimbotPage.Visible  = false
    VisualsPage.Visible = false
    WhitelistPage.Visible = false
    UICustomPage.Visible = false
    UpdatesPage.Visible = false
    AimbotTabBtn.BackgroundColor3  = uiSettings.buttonBg
    VisualsTabBtn.BackgroundColor3 = uiSettings.buttonBg
    WhitelistTabBtn.BackgroundColor3 = uiSettings.buttonBg
    UICustomTabBtn.BackgroundColor3 = uiSettings.buttonBg
    UpdatesTabBtn.BackgroundColor3 = uiSettings.buttonBg
end

AimbotTabBtn.MouseButton1Click:Connect(function()
    resetTabs(); AimbotPage.Visible = true; AimbotTabBtn.BackgroundColor3 = uiSettings.accentStart
end)
VisualsTabBtn.MouseButton1Click:Connect(function()
    resetTabs(); VisualsPage.Visible = true; VisualsTabBtn.BackgroundColor3 = uiSettings.accentStart
end)
WhitelistTabBtn.MouseButton1Click:Connect(function()
    resetTabs(); WhitelistPage.Visible = true; WhitelistTabBtn.BackgroundColor3 = uiSettings.accentStart
end)
UICustomTabBtn.MouseButton1Click:Connect(function()
    resetTabs(); UICustomPage.Visible = true; UICustomTabBtn.BackgroundColor3 = uiSettings.accentStart
end)
UpdatesTabBtn.MouseButton1Click:Connect(function()
    resetTabs(); UpdatesPage.Visible = true; UpdatesTabBtn.BackgroundColor3 = uiSettings.accentStart
end)

-- ==================== AIMBOT PAGE ====================
local StatusCard = Instance.new("Frame")
StatusCard.Size = UDim2.new(1, -10, 0, 32)
StatusCard.Position = UDim2.fromOffset(0, 5)
StatusCard.BackgroundColor3 = uiSettings.activeRedBg
StatusCard.Parent = AimbotPage

local StatusCardCorner = Instance.new("UICorner")
StatusCardCorner.CornerRadius = UDim.new(0, 6)
StatusCardCorner.Parent = StatusCard

local StatusCardStroke = Instance.new("UIStroke")
StatusCardStroke.Color = uiSettings.activeRed
StatusCardStroke.Thickness = 1
StatusCardStroke.Parent = StatusCard

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -20, 1, 0)
StatusLabel.Position = UDim2.fromOffset(10, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Aimbot STATUS: DEACTIVATED"
StatusLabel.TextColor3 = uiSettings.activeRed
StatusLabel.Font = Theme.FontBold
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.TextSize = 12
StatusLabel.Parent = StatusCard

StatusLabel:GetPropertyChangedSignal("TextColor3"):Connect(function()
    local color = StatusLabel.TextColor3
    if color == uiSettings.activeGreen then
        TweenService:Create(StatusCardStroke, TweenInfo.new(0.3), { Color = uiSettings.accentStart }):Play()
    else
        TweenService:Create(StatusCardStroke, TweenInfo.new(0.3), { Color = uiSettings.activeRed }):Play()
    end
end)

local BindLabel = Instance.new("TextLabel")
BindLabel.Size = UDim2.new(0, 100, 0, 22)
BindLabel.Position = UDim2.fromOffset(0, 47)
BindLabel.BackgroundTransparency = 1
BindLabel.Text = "Aimbot Bind:"
BindLabel.TextColor3 = uiSettings.textSecondary
BindLabel.Font = Theme.FontMedium
BindLabel.TextXAlignment = Enum.TextXAlignment.Left
BindLabel.TextSize = 11
BindLabel.Parent = AimbotPage

local KeybindBtn = createButton(AimbotPage, "[ " .. toggleKey.Name .. " ]", UDim2.fromOffset(110, 45), UDim2.fromOffset(120, 25))

local UIBindLabel = Instance.new("TextLabel")
UIBindLabel.Size = UDim2.new(0, 100, 0, 22)
UIBindLabel.Position = UDim2.fromOffset(0, 75)
UIBindLabel.BackgroundTransparency = 1
UIBindLabel.Text = "UI Toggle Bind:"
UIBindLabel.TextColor3 = uiSettings.textSecondary
UIBindLabel.Font = Theme.FontMedium
UIBindLabel.TextXAlignment = Enum.TextXAlignment.Left
UIBindLabel.TextSize = 11
UIBindLabel.Parent = AimbotPage

local UIKeybindBtn = createButton(AimbotPage, "[ " .. uiToggleKey.Name .. " ]", UDim2.fromOffset(110, 73), UDim2.fromOffset(120, 25))

local HoldBindLabel = Instance.new("TextLabel")
HoldBindLabel.Size = UDim2.new(0, 100, 0, 22)
HoldBindLabel.Position = UDim2.fromOffset(0, 103)
HoldBindLabel.BackgroundTransparency = 1
HoldBindLabel.Text = "Hold Key:"
HoldBindLabel.TextColor3 = uiSettings.textSecondary
HoldBindLabel.Font = Theme.FontMedium
HoldBindLabel.TextXAlignment = Enum.TextXAlignment.Left
HoldBindLabel.TextSize = 11
HoldBindLabel.Parent = AimbotPage

local HoldKeybindBtn = createButton(AimbotPage, "[ " .. aimHoldKey.Name .. " ]", UDim2.fromOffset(110, 101), UDim2.fromOffset(120, 25))

createSlider(AimbotPage, "FOV Radius", UDim2.fromOffset(0, 133), fovRadius, 400, function(percent)
    fovRadius = mathClamp(mathRound(percent * 400), 10, 400)
    FOVCircle.Size = UDim2.fromOffset(fovRadius * 2, fovRadius * 2)
    return fovRadius
end)

createSlider(AimbotPage, "Aim Smoothness", UDim2.fromOffset(0, 183), mathRound(locationSpeed * 100), 100, function(percent)
    locationSpeed = mathClamp(percent, 0.02, 1)
    if locationSpeed >= 0.96 then locationSpeed = 1 end
    if locationSpeed == 1 then return "100% (Instant)" else return mathRound(locationSpeed * 100) .. "%" end
end)

local TeamCheckBtn = createButton(AimbotPage, "Team Check: OFF", UDim2.fromOffset(0, 233), UDim2.fromOffset(155, 25))
TeamCheckBtn.MouseButton1Click:Connect(function()
    teamCheckEnabled = not teamCheckEnabled
    TeamCheckBtn.Text = teamCheckEnabled and "Team Check: ON" or "Team Check: OFF"
    TeamCheckBtn.BackgroundColor3 = teamCheckEnabled and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
end)

local Aimbot360Btn = createButton(AimbotPage, "360 Aimbot: OFF", UDim2.fromOffset(165, 233), UDim2.fromOffset(155, 25))
Aimbot360Btn.MouseButton1Click:Connect(function()
    Aimbot360Enabled = not Aimbot360Enabled
    Aimbot360Btn.Text = Aimbot360Enabled and "360 Aimbot: ON" or "360 Aimbot: OFF"
    Aimbot360Btn.BackgroundColor3 = Aimbot360Enabled and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
    FOVCircle.Visible = not Aimbot360Enabled
end)

local SilentAimBtn = createButton(AimbotPage, "Silent Aim: OFF", UDim2.fromOffset(0, 263), UDim2.fromOffset(155, 25))
SilentAimBtn.MouseButton1Click:Connect(function()
    silentAimEnabled = not silentAimEnabled
    SilentAimBtn.Text = silentAimEnabled and "Silent Aim: ON" or "Silent Aim: OFF"
    SilentAimBtn.BackgroundColor3 = silentAimEnabled and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
end)

local HoldModeBtn = createButton(AimbotPage, "Hold Mode: OFF", UDim2.fromOffset(165, 263), UDim2.fromOffset(155, 25))
HoldModeBtn.MouseButton1Click:Connect(function()
    aimHoldActive = not aimHoldActive
    HoldModeBtn.Text = aimHoldActive and "Hold Mode: ON" or "Hold Mode: OFF"
    HoldModeBtn.BackgroundColor3 = aimHoldActive and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
end)

local MobileAimbotMenuBtn = createButton(AimbotPage, "Mobile Aimbot: OFF", UDim2.fromOffset(0, 293), UDim2.fromOffset(320, 25))
MobileAimbotMenuBtn.MouseButton1Click:Connect(function()
    FloatingAimBtn.Visible = not FloatingAimBtn.Visible
    MobileAimbotMenuBtn.Text = FloatingAimBtn.Visible and "Mobile Aimbot: ON" or "Mobile Aimbot: OFF"
    MobileAimbotMenuBtn.BackgroundColor3 = FloatingAimBtn.Visible and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
end)

-- ==================== VISUALS PAGE ====================
local MasterESPBtn = createButton(VisualsPage, "Master ESP: OFF",       UDim2.fromOffset(0, 5), UDim2.fromOffset(155, 25))
local BoxESPBtn    = createButton(VisualsPage, "Chams: OFF",             UDim2.fromOffset(165, 5), UDim2.fromOffset(155, 25))
local NameESPBtn   = createButton(VisualsPage, "Names: OFF",  UDim2.fromOffset(0, 35), UDim2.fromOffset(155, 25))
local WallCheckESPBtn = createButton(VisualsPage, "ESP Wallcheck: OFF", UDim2.fromOffset(165, 35), UDim2.fromOffset(155, 25))

createGradientPicker(VisualsPage, "Box Outline:", UDim2.fromOffset(0, 68), espBoxColor, function(color)
    espBoxColor = color
    if espMasterEnabled and espShowBoxes then updateESPConfig() end
end)

createGradientPicker(VisualsPage, "Box Fill:", UDim2.fromOffset(0, 153), espBoxFillColor, function(color)
    espBoxFillColor = color
    if espMasterEnabled and espShowBoxes then updateESPConfig() end
end)

createGradientPicker(VisualsPage, "Name Color:", UDim2.fromOffset(0, 238), espNameColor, function(color)
    espNameColor = color
    if espMasterEnabled and espShowNames then updateESPConfig() end
end)

createSlider(VisualsPage, "Fill Transp", UDim2.fromOffset(0, 323), chamsFillTransparency * 100, 100, function(percent)
    chamsFillTransparency = percent
    if espMasterEnabled and espShowBoxes then updateESPConfig() end
    return mathRound(percent * 100) .. "%"
end)

-- ==================== WHITELIST PAGE ====================
local WhitelistToggle = createButton(WhitelistPage, "Whitelist: OFF", UDim2.fromOffset(0, 5), UDim2.fromOffset(320, 25))
WhitelistToggle.MouseButton1Click:Connect(function()
    whitelistEnabled = not whitelistEnabled
    WhitelistToggle.Text = whitelistEnabled and "Whitelist: ON" or "Whitelist: OFF"
    WhitelistToggle.BackgroundColor3 = whitelistEnabled and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
end)

local SaveWhitelistBtn = createButton(WhitelistPage, "Save Whitelist", UDim2.fromOffset(0, 35), UDim2.fromOffset(320, 25))
SaveWhitelistBtn.MouseButton1Click:Connect(function()
    saveWhitelist()
    SaveWhitelistBtn.Text = "Saved!"
    SaveWhitelistBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
    task.wait(0.5)
    SaveWhitelistBtn.Text = "Save Whitelist"
    SaveWhitelistBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
end)

local SearchBox = Instance.new("TextBox")
SearchBox.Size = UDim2.fromOffset(200, 25)
SearchBox.Position = UDim2.fromOffset(0, 68)
SearchBox.BackgroundColor3 = uiSettings.inputBg
SearchBox.TextColor3 = uiSettings.textPrimary
SearchBox.Font = Theme.FontMedium
SearchBox.TextSize = 11
SearchBox.PlaceholderText = "Search player name..."
SearchBox.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
SearchBox.Text = ""
SearchBox.Parent = WhitelistPage

local SearchCorner = Instance.new("UICorner")
SearchCorner.CornerRadius = UDim.new(0, 6)
SearchCorner.Parent = SearchBox

local SearchStroke = Instance.new("UIStroke")
SearchStroke.Color = uiSettings.accentStart
SearchStroke.Thickness = 1
SearchStroke.Parent = SearchBox

local SearchResults = Instance.new("ScrollingFrame")
SearchResults.Size = UDim2.new(1, -10, 0, 90)
SearchResults.Position = UDim2.fromOffset(0, 98)
SearchResults.BackgroundColor3 = uiSettings.containerBg
SearchResults.BorderSizePixel = 0
SearchResults.ScrollBarThickness = 4
SearchResults.CanvasSize = UDim2.fromScale(0, 1)
SearchResults.Parent = WhitelistPage

local SearchResultsCorner = Instance.new("UICorner")
SearchResultsCorner.CornerRadius = UDim.new(0, 6)
SearchResultsCorner.Parent = SearchResults

local ResultsList = Instance.new("UIListLayout")
ResultsList.SortOrder = Enum.SortOrder.LayoutOrder
ResultsList.Padding = UDim.new(0, 2)
ResultsList.Parent = SearchResults

local WhitelistLabel = Instance.new("TextLabel")
WhitelistLabel.Size = UDim2.new(1, -10, 0, 22)
WhitelistLabel.Position = UDim2.fromOffset(0, 193)
WhitelistLabel.BackgroundTransparency = 1
WhitelistLabel.Text = "Whitelisted Players:"
WhitelistLabel.TextColor3 = uiSettings.textSecondary
WhitelistLabel.Font = Theme.FontBold
WhitelistLabel.TextXAlignment = Enum.TextXAlignment.Left
WhitelistLabel.TextSize = 11
WhitelistLabel.Parent = WhitelistPage

local WhitelistContainer = Instance.new("ScrollingFrame")
WhitelistContainer.Size = UDim2.new(1, -10, 0, 85)
WhitelistContainer.Position = UDim2.fromOffset(0, 218)
WhitelistContainer.BackgroundColor3 = uiSettings.containerBg
WhitelistContainer.BorderSizePixel = 0
WhitelistContainer.ScrollBarThickness = 4
WhitelistContainer.CanvasSize = UDim2.fromScale(0, 1)
WhitelistContainer.Parent = WhitelistPage

local WhitelistContainerCorner = Instance.new("UICorner")
WhitelistContainerCorner.CornerRadius = UDim.new(0, 6)
WhitelistContainerCorner.Parent = WhitelistContainer

local WhitelistItemsLayout = Instance.new("UIListLayout")
WhitelistItemsLayout.SortOrder = Enum.SortOrder.LayoutOrder
WhitelistItemsLayout.Padding = UDim.new(0, 2)
WhitelistItemsLayout.Parent = WhitelistContainer

-- ==================== UI CUSTOMIZATION PAGE (SCROLLABLE) ====================
local UICustomScroll = Instance.new("ScrollingFrame")
UICustomScroll.Size = UDim2.new(1, 0, 1, 0)
UICustomScroll.Position = UDim2.fromScale(0, 0)
UICustomScroll.BackgroundTransparency = 1
UICustomScroll.BorderSizePixel = 0
UICustomScroll.ScrollBarThickness = 4
UICustomScroll.CanvasSize = UDim2.fromOffset(0, 750)
UICustomScroll.Parent = UICustomPage

local UICustomList = Instance.new("UIListLayout")
UICustomList.SortOrder = Enum.SortOrder.LayoutOrder
UICustomList.Padding = UDim.new(0, 2)
UICustomList.Parent = UICustomScroll

local SaveUIBtn = createButton(UICustomScroll, "Save UI Theme", UDim2.fromOffset(0, 0), UDim2.fromOffset(320, 25))
SaveUIBtn.MouseButton1Click:Connect(function()
    saveUISettings()
    SaveUIBtn.Text = "Saved!"
    SaveUIBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
    task.wait(0.5)
    SaveUIBtn.Text = "Save UI Theme"
    SaveUIBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
end)

local ResetUIBtn = createButton(UICustomScroll, "Reset to Default", UDim2.fromOffset(0, 0), UDim2.fromOffset(320, 25))
ResetUIBtn.MouseButton1Click:Connect(function()
    uiSettings = {
        background = Color3.fromRGB(12, 12, 16),
        sidebarBackground = Color3.fromRGB(18, 18, 24),
        headerBackground = Color3.fromRGB(24, 24, 32),
        border = Color3.fromRGB(50, 50, 60),
        accentStart = Color3.fromRGB(255, 255, 255),
        accentEnd = Color3.fromRGB(80, 80, 80),
        textPrimary = Color3.fromRGB(255, 255, 255),
        textSecondary = Color3.fromRGB(160, 160, 170),
        activeRed = Color3.fromRGB(200, 200, 200),
        activeGreen = Color3.fromRGB(255, 255, 255),
        activeRedBg = Color3.fromRGB(22, 22, 26),
        buttonBg = Color3.fromRGB(24, 24, 30),
        buttonActiveBg = Color3.fromRGB(240, 240, 250),
        buttonActiveText = Color3.fromRGB(15, 15, 20),
        trackBg = Color3.fromRGB(35, 35, 45),
        inputBg = Color3.fromRGB(10, 10, 10),
        containerBg = Color3.fromRGB(20, 20, 26),
        itemBg = Color3.fromRGB(30, 30, 38),
        itemHoverBg = Color3.fromRGB(50, 50, 60),
        mainTransparency = 0,
        sidebarTransparency = 0,
        headerTransparency = 0,
    }
    saveUISettings()
    refreshAllUI()
end)

createSlider(UICustomScroll, "Main Transp", UDim2.fromOffset(0, 0), uiSettings.mainTransparency * 100, 100, function(percent)
    uiSettings.mainTransparency = percent
    MainFrame.BackgroundTransparency = percent
    return mathRound(percent * 100) .. "%"
end)

createSlider(UICustomScroll, "Sidebar Transp", UDim2.fromOffset(0, 0), uiSettings.sidebarTransparency * 100, 100, function(percent)
    uiSettings.sidebarTransparency = percent
    Sidebar.BackgroundTransparency = percent
    SidebarOverlap.BackgroundTransparency = percent
    SidebarOverlapBottom.BackgroundTransparency = percent
    return mathRound(percent * 100) .. "%"
end)

createSlider(UICustomScroll, "Header Transp", UDim2.fromOffset(0, 0), uiSettings.headerTransparency * 100, 100, function(percent)
    uiSettings.headerTransparency = percent
    HeaderFrame.BackgroundTransparency = percent
    HeaderOverlap.BackgroundTransparency = percent
    return mathRound(percent * 100) .. "%"
end)

createGradientPicker(UICustomScroll, "Background:", UDim2.fromOffset(0, 0), uiSettings.background, function(color)
    uiSettings.background = color
    MainFrame.BackgroundColor3 = color
end)

createGradientPicker(UICustomScroll, "Sidebar BG:", UDim2.fromOffset(0, 0), uiSettings.sidebarBackground, function(color)
    uiSettings.sidebarBackground = color
    Sidebar.BackgroundColor3 = color
    SidebarOverlap.BackgroundColor3 = color
    SidebarOverlapBottom.BackgroundColor3 = color
end)

createGradientPicker(UICustomScroll, "Header BG:", UDim2.fromOffset(0, 0), uiSettings.headerBackground, function(color)
    uiSettings.headerBackground = color
    HeaderFrame.BackgroundColor3 = color
    HeaderOverlap.BackgroundColor3 = color
end)

createGradientPicker(UICustomScroll, "Border:", UDim2.fromOffset(0, 0), uiSettings.border, function(color)
    uiSettings.border = color
    HeaderDivider.BackgroundColor3 = color
    SidebarDivider.BackgroundColor3 = color
end)

createGradientPicker(UICustomScroll, "Accent:", UDim2.fromOffset(0, 0), uiSettings.accentStart, function(color)
    uiSettings.accentStart = color
    refreshAllUI()
end)

createGradientPicker(UICustomScroll, "Text Primary:", UDim2.fromOffset(0, 0), uiSettings.textPrimary, function(color)
    uiSettings.textPrimary = color
    HeaderLabel.TextColor3 = color
end)

createGradientPicker(UICustomScroll, "Text Secondary:", UDim2.fromOffset(0, 0), uiSettings.textSecondary, function(color)
    uiSettings.textSecondary = color
end)

-- ==================== UPDATES/NEWS PAGE ====================
local UpdatesScroll = Instance.new("ScrollingFrame")
UpdatesScroll.Size = UDim2.new(1, -10, 1, -40)
UpdatesScroll.Position = UDim2.fromOffset(0, 0)
UpdatesScroll.BackgroundColor3 = uiSettings.containerBg
UpdatesScroll.BorderSizePixel = 0
UpdatesScroll.ScrollBarThickness = 4
UpdatesScroll.CanvasSize = UDim2.fromScale(0, 1)
UpdatesScroll.Parent = UpdatesPage

local UpdatesCorner = Instance.new("UICorner")
UpdatesCorner.CornerRadius = UDim.new(0, 6)
UpdatesCorner.Parent = UpdatesScroll

local UpdatesTextLabel = Instance.new("TextLabel")
UpdatesTextLabel.Size = UDim2.new(1, -20, 0, 500)
UpdatesTextLabel.Position = UDim2.fromOffset(10, 10)
UpdatesTextLabel.BackgroundTransparency = 1
UpdatesTextLabel.Text = "Loading updates..."
UpdatesTextLabel.TextColor3 = uiSettings.textPrimary
UpdatesTextLabel.Font = Theme.FontMedium
UpdatesTextLabel.TextSize = 12
UpdatesTextLabel.TextXAlignment = Enum.TextXAlignment.Left
UpdatesTextLabel.TextYAlignment = Enum.TextYAlignment.Top
UpdatesTextLabel.TextWrapped = true
UpdatesTextLabel.RichText = true
UpdatesTextLabel.Parent = UpdatesScroll

local RefreshUpdatesBtn = createButton(UpdatesPage, "Refresh Updates", UDim2.fromOffset(0, 0), UDim2.fromOffset(320, 25))
RefreshUpdatesBtn.Position = UDim2.new(0, 5, 1, -30)

-- Fetch updates function
local function fetchUpdates()
    UpdatesTextLabel.Text = "Loading updates..."
    local success, result = pcall(function()
        return game:HttpGet(UPDATE_NEWS_LINK)
    end)
    if success and result then
        UpdatesTextLabel.Text = result
        UpdatesTextLabel.Size = UDim2.new(1, -20, 0, 0)
        UpdatesTextLabel.Size = UDim2.new(1, -20, 0, UpdatesTextLabel.TextBounds.Y + 20)
        UpdatesScroll.CanvasSize = UDim2.fromOffset(0, UpdatesTextLabel.TextBounds.Y + 40)
    else
        UpdatesTextLabel.Text = "Failed to load updates.\nPlease check the link or your internet connection.\n\nYou can change the link in the script by editing UPDATE_NEWS_LINK variable."
        UpdatesTextLabel.Size = UDim2.new(1, -20, 0, 150)
        UpdatesScroll.CanvasSize = UDim2.fromOffset(0, 150)
    end
end

RefreshUpdatesBtn.MouseButton1Click:Connect(fetchUpdates)

-- Auto-fetch on open
task.spawn(fetchUpdates)

-- ==================== CORE FUNCTIONS ====================
function toggleAimbot()
    AimbotEnabled = not AimbotEnabled
    if AimbotEnabled then
        StatusLabel.Text = "Aimbot STATUS: ACTIVE"
        StatusLabel.TextColor3 = uiSettings.activeGreen
        FloatingAimBtn.TextColor3 = Color3.fromRGB(15, 15, 20)
        FloatingAimBtn.BackgroundColor3 = Color3.fromRGB(240, 240, 250)
        FloatingStroke.Color = uiSettings.accentStart
    else
        StatusLabel.Text = "Aimbot STATUS: DEACTIVATED"
        StatusLabel.TextColor3 = uiSettings.activeRed
        FloatingAimBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
        FloatingAimBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
        FloatingStroke.Color = Color3.fromRGB(255, 255, 255)
        targetPlayer = nil
        targetHead = nil
    end
end

makeDraggable(FloatingAimBtn, function() toggleAimbot() end)
FloatingAimBtn.MouseEnter:Connect(function()
    if not AimbotEnabled then
        TweenService:Create(FloatingAimBtn, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(40, 40, 48) }):Play()
    end
end)
FloatingAimBtn.MouseLeave:Connect(function()
    if not AimbotEnabled then
        TweenService:Create(FloatingAimBtn, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(20, 20, 24) }):Play()
    end
end)

KeybindBtn.MouseButton1Click:Connect(function()
    listeningForKey = true
    KeybindBtn.Text = "... Press Key ..."
    KeybindBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 40)
end)
UIKeybindBtn.MouseButton1Click:Connect(function()
    listeningForUIKey = true
    UIKeybindBtn.Text = "... Press Key ..."
    UIKeybindBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 40)
end)
HoldKeybindBtn.MouseButton1Click:Connect(function()
    listeningForHoldKey = true
    HoldKeybindBtn.Text = "... Press Key ..."
    HoldKeybindBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 40)
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if listeningForKey and input.UserInputType == Enum.UserInputType.Keyboard then
        toggleKey = input.KeyCode
        listeningForKey = false
        KeybindBtn.Text = "[ " .. toggleKey.Name .. " ]"
        KeybindBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        return
    end
    if listeningForUIKey and input.UserInputType == Enum.UserInputType.Keyboard then
        uiToggleKey = input.KeyCode
        listeningForUIKey = false
        UIKeybindBtn.Text = "[ " .. uiToggleKey.Name .. " ]"
        UIKeybindBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        return
    end
    if listeningForHoldKey and input.UserInputType == Enum.UserInputType.Keyboard then
        aimHoldKey = input.KeyCode
        listeningForHoldKey = false
        HoldKeybindBtn.Text = "[ " .. aimHoldKey.Name .. " ]"
        HoldKeybindBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        return
    end
    if UserInputService:GetFocusedTextBox() then return end
    
    if input.KeyCode == toggleKey and not aimHoldActive then
        toggleAimbot()
    elseif input.KeyCode == uiToggleKey then
        if MainFrame.Visible then closeMenu() else openMenu() end
    elseif input.KeyCode == aimHoldKey and aimHoldActive then
        holdKeyDown = true
        if not AimbotEnabled then toggleAimbot() end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == aimHoldKey and aimHoldActive then
        holdKeyDown = false
        if AimbotEnabled then toggleAimbot() end
    end
end)

-- ==================== AIMBOT SYSTEM ====================
local raycastParams = RaycastParams.new()
raycastParams.FilterType = Enum.RaycastFilterType.Exclude
raycastParams.IgnoreWater = true

local function getCharacterHead(character)
    if not character then return nil end
    local head = character:FindFirstChild("Head")
    if head and head:IsA("BasePart") then return head end
    for _, part in character:GetChildren() do
        if part:IsA("BasePart") and part.Name:lower() == "head" then return part end
    end
    return nil
end

local function hasLineOfSight(targetCharacter, targetHead)
    local origin = Camera.CFrame.Position
    local filterInstances = tableCreate(8)
    local localChar = LocalPlayer.Character
    if localChar then filterInstances[1] = localChar end
    local count = 1
    for _, obj in targetCharacter:GetChildren() do
        if obj:IsA("BasePart") and obj.Transparency >= 0.5 then
            count += 1
            filterInstances[count] = obj
        end
    end
    raycastParams.FilterDescendantsInstances = filterInstances
    local direction = targetHead.Position - origin
    local result = Workspace:Raycast(origin, direction, raycastParams)
    return not result or result.Instance:IsDescendantOf(targetCharacter)
end

local function getClosestTarget()
    local closestCharacter = nil
    local shortestDistanceSq = mathHuge
    local camera = Camera
    local localPlayer = LocalPlayer
    local localTeam = localPlayer.Team
    local cameraPosition = camera.CFrame.Position
    local viewportSize = camera.ViewportSize
    local centerX = viewportSize.X * 0.5
    local centerY = viewportSize.Y * 0.5
    local fovRadiusSq = fovRadius * fovRadius
    local players = Players:GetPlayers()
    
    for i = 1, #players do
        local player = players[i]
        if player ~= localPlayer then
            if whitelistEnabled and whitelistData[tostring(player.UserId)] then continue end
            local character = player.Character
            if character and not (teamCheckEnabled and localTeam and player.Team == localTeam) then
                local head = getCharacterHead(character)
                if head then
                    local humanoid = character:FindFirstChildOfClass("Humanoid")
                    if humanoid and humanoid.Health > 0 then
                        local headPosition = head.Position
                        if hasLineOfSight(character, head) then
                            if Aimbot360Enabled then
                                local offset = headPosition - cameraPosition
                                local distanceSq = offset:Dot(offset)
                                if distanceSq < shortestDistanceSq then
                                    shortestDistanceSq = distanceSq
                                    closestCharacter = character
                                end
                            else
                                local screenPos, onScreen = camera:WorldToViewportPoint(headPosition)
                                if onScreen then
                                    local dx = screenPos.X - centerX
                                    local dy = screenPos.Y - centerY
                                    local distanceSq = dx * dx + dy * dy
                                    if distanceSq <= fovRadiusSq and distanceSq < shortestDistanceSq then
                                        shortestDistanceSq = distanceSq
                                        closestCharacter = character
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    return closestCharacter
end

-- ==================== ESP SYSTEM ====================
local espConnections = {}
local espUpdateConnections = {}

local function clearESPElements(character)
    if character:FindFirstChild("ESPHighlight") then character.ESPHighlight:Destroy() end
    if character:FindFirstChild("ESPBillboard") then character.ESPBillboard:Destroy() end
end

local function isBehindWall(character)
    local head = getCharacterHead(character)
    if not head then return true end
    local cameraPos = Camera.CFrame.Position
    local direction = head.Position - cameraPos
    local espRayParams = RaycastParams.new()
    espRayParams.FilterDescendantsInstances = {LocalPlayer.Character, character}
    espRayParams.FilterType = Enum.RaycastFilterType.Exclude
    local result = Workspace:Raycast(cameraPos, direction, espRayParams)
    return result ~= nil
end

local function updateESPWallcheck(character, highlight, billboard)
    if not espWallCheck then
        if highlight then highlight.Enabled = true end
        if billboard then billboard.Enabled = true end
        return
    end
    local behindWall = isBehindWall(character)
    if highlight then highlight.Enabled = not behindWall end
    if billboard then billboard.Enabled = not behindWall end
end

local function applyESPToPlayer(player)
    if espConnections[player] then espConnections[player]:Disconnect() end
    if espUpdateConnections[player] then espUpdateConnections[player]:Disconnect() end
    
    local function onCharacterRender(character)
        clearESPElements(character)
        if not espMasterEnabled then return end
        local highlight = nil
        local billboard = nil
        
        if not (espWallCheck and isBehindWall(character)) then
            if espShowBoxes then
                highlight = Instance.new("Highlight")
                highlight.Name = "ESPHighlight"
                highlight.FillColor = espBoxFillColor
                highlight.FillTransparency = chamsFillTransparency
                highlight.OutlineColor = espBoxColor
                highlight.OutlineTransparency = chamsOutlineTransparency
                highlight.Adornee = character
                highlight.Parent = character
            end
            if espShowNames then
                local head = getCharacterHead(character)
                if head then
                    billboard = Instance.new("BillboardGui")
                    billboard.Name = "ESPBillboard"
                    billboard.AlwaysOnTop = true
                    billboard.Size = UDim2.fromOffset(120, 35)
                    billboard.StudsOffset = Vector3.new(0, 3, 0)
                    billboard.Adornee = head
                    local label = Instance.new("TextLabel")
                    label.Size = UDim2.fromScale(1, 1)
                    label.BackgroundTransparency = 1
                    label.TextColor3 = espNameColor
                    label.TextStrokeTransparency = 0
                    label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                    label.Font = Theme.FontBold
                    label.TextSize = 12
                    label.Parent = billboard
                    billboard.Parent = character
                    local humanoid = character:FindFirstChildOfClass("Humanoid")
                    if humanoid then
                        local function syncStats()
                            if character.Parent and humanoid.Parent then
                                label.Text = string.format("%s\n[ HP: %d ]", player.Name, mathRound(humanoid.Health))
                                updateESPWallcheck(character, highlight, billboard)
                            end
                        end
                        humanoid:GetPropertyChangedSignal("Health"):Connect(syncStats)
                        syncStats()
                    end
                end
            end
        end
        
        if espWallCheck then
            espUpdateConnections[player] = RunService.RenderStepped:Connect(function()
                if character and character.Parent then
                    updateESPWallcheck(character, highlight, billboard)
                else
                    if espUpdateConnections[player] then
                        espUpdateConnections[player]:Disconnect()
                        espUpdateConnections[player] = nil
                    end
                end
            end)
        end
    end
    
    espConnections[player] = player.CharacterAdded:Connect(onCharacterRender)
    if player.Character then task.spawn(onCharacterRender, player.Character) end
end

function updateESPConfig()
    for player, conn in pairs(espUpdateConnections) do
        conn:Disconnect()
        espUpdateConnections[player] = nil
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            if player.Character then task.spawn(clearESPElements, player.Character) end
            if espMasterEnabled then applyESPToPlayer(player) end
        end
    end
end

MasterESPBtn.MouseButton1Click:Connect(function()
    espMasterEnabled = not espMasterEnabled
    MasterESPBtn.Text = espMasterEnabled and "Master ESP: ON" or "Master ESP: OFF"
    MasterESPBtn.BackgroundColor3 = espMasterEnabled and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
    updateESPConfig()
end)
BoxESPBtn.MouseButton1Click:Connect(function()
    espShowBoxes = not espShowBoxes
    BoxESPBtn.Text = espShowBoxes and "Chams: ON" or "Chams: OFF"
    BoxESPBtn.BackgroundColor3 = espShowBoxes and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
    updateESPConfig()
end)
NameESPBtn.MouseButton1Click:Connect(function()
    espShowNames = not espShowNames
    NameESPBtn.Text = espShowNames and "Names: ON" or "Names: OFF"
    NameESPBtn.BackgroundColor3 = espShowNames and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
    updateESPConfig()
end)
WallCheckESPBtn.MouseButton1Click:Connect(function()
    espWallCheck = not espWallCheck
    WallCheckESPBtn.Text = espWallCheck and "ESP Wallcheck: ON" or "ESP Wallcheck: OFF"
    WallCheckESPBtn.BackgroundColor3 = espWallCheck and Color3.fromRGB(0, 120, 200) or Color3.fromRGB(30, 30, 40)
    updateESPConfig()
end)

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function()
        task.wait(0.5)
        if espMasterEnabled then applyESPToPlayer(player) end
    end)
end)
Players.PlayerRemoving:Connect(function(player)
    if espConnections[player] then espConnections[player]:Disconnect(); espConnections[player] = nil end
    if espUpdateConnections[player] then espUpdateConnections[player]:Disconnect(); espUpdateConnections[player] = nil end
    if player.Character then clearESPElements(player.Character) end
end)

-- ==================== SILENT AIM ====================
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local args = {...}
    local method = getnamecallmethod()
    if silentAimEnabled and targetHead and (method == "FindPartOnRayWithWhitelist" or method == "FindPartOnRayWithIgnoreList") then
        local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if tool and self == tool then
            local origin = args[1]
            if typeof(origin) == "Ray" then
                local newDirection = (targetHead.Position - origin.Origin).Unit * origin.Direction.Magnitude
                args[1] = Ray.new(origin.Origin, newDirection)
            end
        end
    end
    return oldNamecall(self, unpack(args))
end)

-- ==================== WHITELIST FUNCTIONS ====================
local function updateWhitelistDisplay()
    for _, child in WhitelistContainer:GetChildren() do
        if child:IsA("Frame") then child:Destroy() end
    end
    local yOffset = 0
    for userId, name in pairs(whitelistData) do
        local itemFrame = Instance.new("Frame")
        itemFrame.Size = UDim2.new(1, 0, 0, 22)
        itemFrame.BackgroundColor3 = uiSettings.itemBg
        itemFrame.Parent = WhitelistContainer
        local itemCorner = Instance.new("UICorner")
        itemCorner.CornerRadius = UDim.new(0, 4)
        itemCorner.Parent = itemFrame
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, -35, 1, 0)
        nameLabel.Position = UDim2.fromOffset(10, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = name .. " (" .. userId .. ")"
        nameLabel.TextColor3 = uiSettings.textPrimary
        nameLabel.Font = Theme.FontMedium
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.TextSize = 11
        nameLabel.Parent = itemFrame
        local removeBtn = Instance.new("TextButton")
        removeBtn.Size = UDim2.fromOffset(22, 22)
        removeBtn.Position = UDim2.new(1, -27, 0, 0)
        removeBtn.BackgroundTransparency = 1
        removeBtn.Text = "×"
        removeBtn.TextColor3 = Color3.fromRGB(200, 50, 50)
        removeBtn.Font = Theme.FontBold
        removeBtn.TextSize = 16
        removeBtn.Parent = itemFrame
        removeBtn.MouseButton1Click:Connect(function()
            whitelistData[userId] = nil
            saveWhitelist()
            updateWhitelistDisplay()
        end)
        yOffset = yOffset + 24
    end
    WhitelistContainer.CanvasSize = UDim2.fromOffset(0, math.max(yOffset, 85))
end

local function searchPlayers(query)
    for _, child in SearchResults:GetChildren() do
        if child:IsA("Frame") then child:Destroy() end
    end
    if query == "" then
        SearchResults.CanvasSize = UDim2.fromScale(0, 1)
        return
    end
    local queryLower = query:lower()
    local foundPlayers = {}
    for _, player in Players:GetPlayers() do
        if player ~= LocalPlayer then
            local nameLower = player.Name:lower()
            local displayLower = (player.DisplayName or ""):lower()
            if nameLower:find(queryLower, 1, true) or displayLower:find(queryLower, 1, true) then
                table.insert(foundPlayers, {name = player.Name, displayName = player.DisplayName, userId = player.UserId})
            end
        end
    end
    table.sort(foundPlayers, function(a, b) return #a.name:lower() < #b.name:lower() end)
    local maxResults = math.min(#foundPlayers, 15)
    local yOffset = 0
    for i = 1, maxResults do
        local player = foundPlayers[i]
        local resultFrame = Instance.new("Frame")
        resultFrame.Size = UDim2.new(1, 0, 0, 22)
        resultFrame.BackgroundColor3 = uiSettings.itemBg
        resultFrame.Parent = SearchResults
        local resultCorner = Instance.new("UICorner")
        resultCorner.CornerRadius = UDim.new(0, 4)
        resultCorner.Parent = resultFrame
        local resultText = Instance.new("TextButton")
        resultText.Size = UDim2.new(1, 0, 1, 0)
        resultText.BackgroundTransparency = 1
        resultText.Text = player.name .. (player.displayName ~= player.name and " (" .. player.displayName .. ")" or "")
        resultText.TextColor3 = uiSettings.textPrimary
        resultText.Font = Theme.FontMedium
        resultText.TextXAlignment = Enum.TextXAlignment.Left
        resultText.TextSize = 11
        resultText.Parent = resultFrame
        resultText.MouseButton1Click:Connect(function()
            whitelistData[tostring(player.userId)] = player.name
            saveWhitelist()
            updateWhitelistDisplay()
        end)
        resultFrame.MouseEnter:Connect(function() resultFrame.BackgroundColor3 = uiSettings.itemHoverBg end)
        resultFrame.MouseLeave:Connect(function() resultFrame.BackgroundColor3 = uiSettings.itemBg end)
        yOffset = yOffset + 24
    end
    SearchResults.CanvasSize = UDim2.fromOffset(0, math.max(yOffset, 90))
end

SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    searchPlayers(SearchBox.Text)
end)

-- ==================== MAIN LOOPS ====================
task.spawn(function()
    while true do
        if AimbotEnabled then
            targetPlayer = getClosestTarget()
            if targetPlayer then targetHead = getCharacterHead(targetPlayer) else targetHead = nil end
        else
            targetPlayer = nil
            targetHead = nil
        end
        task.wait(0.05)
    end
end)

RunService.RenderStepped:Connect(function()
    if not AimbotEnabled or not targetHead then return end
    local currentCFrame = Camera.CFrame
    local targetRotation = cframeLookAt(currentCFrame.Position, targetHead.Position)
    if locationSpeed >= 1 then
        Camera.CFrame = targetRotation
    else
        Camera.CFrame = currentCFrame:Lerp(targetRotation, locationSpeed)
    end
end)

updateWhitelistDisplay()
